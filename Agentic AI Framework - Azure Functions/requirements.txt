# DO NOT include azure-functions-worker in this file
# The Python Worker is managed by Azure Functions platform
# Manually managing azure-functions-worker may cause unexpected issues

azure-functions
requests

# SOAP and Azure Key Vault dependencies
zeep
lxml
cryptography
azure-keyvault-certificates
azure-identity
python-dotenv

# JWT signature verification
PyJWT>=2.8.0

# Durable Functions
azure-functions-durable>=1.2.0

# Queue producer (queue trigger itself is handled by the Functions runtime)
azure-storage-queue>=12.6.0

# Azure Table Storage (NPV archive history)
azure-data-tables>=12.5.0

# AI agents (Claude Opus 4.8 on Microsoft Foundry)
anthropic>=0.39.0
pydantic>=2.6.0
httpx>=0.27.0
pypdf>=4.0.0

# Image OCR (Azure AI Document Intelligence — prebuilt-read on AIServices account)
azure-ai-documentintelligence>=1.0.0
